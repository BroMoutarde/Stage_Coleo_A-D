---
title: "Analyses_donnees_2024"
author: "Adrien Debout"
date: "2025-04-30"
output: html_document
editor_options: 
  chunk_output_type: console
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# 1. Environnement

```{r}
library(here) # environnement de travail
library(readxl)
library(dplyr) # mise en forme des tableaux
library(tidyverse)
library(stringr)
library(ggplot2) # ggplot graphiques
library(gridExtra) # Afficher plusieurs ggplots en meme temps
library(fitdistrplus) # Regarder la distribution la mieux adaptée
library(glmmTMB) # modèles mixtes
library(AICcmodavg) # Comparaison de modèles
library(fitdistrplus) # Distribution la mieux adaptée
library(DHARMa) # Vérification des présuposés des modèles
library(emmeans)
library(multcomp)
library(vegan)
library(tidyr)
```

# 2. working directory

```{r}
  # Working diretory
setwd(here::here("Data", "Data entomo"))
here()
```

# 3. Tests & graphs initiaux

```{r}
  # IMPORT Jeu de données coléoptères
Coleo <- read.table("jeu_donnees_coleo.txt", sep = "\t", dec = ".", quote = "\"", stringsAsFactors = F, header = T) %>%
  select(6 : 7, 10 : 20)

  # Calcul de l'abondance par piège
abondance <- Coleo %>%
  group_by(Numero_de_piege) %>%
  summarise(Abondance = sum(Abondance))
  # Graphique de visualisation 
ab <- ggplot(abondance, aes(x = Numero_de_piege, y = Abondance)) +
  geom_bar(stat = "identity", fill = c("darkgreen", "#458B00", "#556B2F", "#9ACD32")) +
  geom_text(aes(label = Abondance), vjust = -0.5, size = 3) +
  labs(title = "Abondance de coléoptères saproxyliques le long des arbres Thèses",
       x = "Pièges entomologiques",
       y = "Abondance (n)") +
  ylim(0, 10000) +
  theme_minimal() +
  theme(plot.title = element_text(size = 9, face = "bold"),
    axis.text.x = element_text(angle = 0))

# Div1 <- Coleo %>%
#   filter(Numero_de_piege == "1") %>%
#   distinct(SP, .keep_all = T) %>%
#   select(9, 13) %>%
#   mutate(nbesp = 1) %>%
#   summarise(nbesp = sum(nbesp)) %>%
#   mutate(piege = "Piege_1")
# Div2 <- Coleo %>%
#   filter(Numero_de_piege == "2") %>%
#   distinct(SP, .keep_all = T) %>%
#   select(9, 13) %>%
#   mutate(nbesp = 1) %>%
#   summarise(nbesp = sum(nbesp)) %>%
#   mutate(piege = "Piege_2")
# Div3 <- Coleo %>%
#   filter(Numero_de_piege == "3") %>%
#   distinct(SP, .keep_all = T) %>%
#   select(9, 13) %>%
#   mutate(nbesp = 1) %>%
#   summarise(nbesp = sum(nbesp)) %>%
#   mutate(piege = "Piege_3")
# Div4 <- Coleo %>%
#   filter(Numero_de_piege == "4") %>%
#   distinct(SP, .keep_all = T) %>%
#   select(9, 13) %>%
#   mutate(nbesp = 1) %>%
#   summarise(nbesp = sum(nbesp)) %>%
#   mutate(piege = "Piege_4")
# Div <- bind_rows(Div1, Div2, Div3, Div4) %>%
#   arrange(piege)

  # 
# div_coleo <- Coleo %>%
#   distinct(SP, .keep_all = T) %>%
#   select(9, 13) %>%
#   mutate(SP = ifelse(SP == "", NA, SP),
#          nbesp = 1) %>%
#   filter(!is.na(SP)) %>%
#   group_by(Numero_de_piege) %>%
#   summarise(nbesp = sum(nbesp))


div_coleo <- Coleo %>%
  distinct(Espece, Numero_de_piege, .keep_all = T) %>%
  select(6, 11, 13)
#
div1 <- div_coleo %>%
  filter(Numero_de_piege == 1)
#
div2 <- div_coleo %>%
  filter(Numero_de_piege == 2)
#
div3 <- div_coleo %>%
  filter(Numero_de_piege == 3)
#
div4 <- div_coleo %>%
  filter(Numero_de_piege == 4)

#

div <- ggplot(div_coleo, aes(x = Numero_de_piege, y = nbesp)) +
  geom_bar(stat = "identity", fill = c("darkgreen", "#458B00", "#556B2F", "#9ACD32")) +
  geom_text(aes(label = nbesp), vjust = -0.5, size = 3) +
  labs(title = "Diversité spécifique dans chaque pièges des arbres Thèses",
       x = "Pièges entomologiques",
       y = "Diversité spécifique") +
  ylim(0, 100) +
  theme_minimal() +
  theme(plot.title = element_text(size = 9, face = "bold"),
        axis.text.x = element_text(angle = 0))
  # Faire apparaître 2 ggplot en même temps
grid.arrange(ab, div, ncol = 2)

#

Coleobis <- Coleo %>%
  select(8, 9, 11, 13) %>%
  mutate(code_sp = ifelse((is.na(GEN) | GEN == "") & (is.na(SP) | SP == ""), "INC",
         paste(GEN, SP, sep = "_"))) %>%
  filter(!is.na(code_sp)) # Vérif des NAs

Coleobis<- Coleobis %>%
  setNames(c("GEN", "SP", "AB", "Num_piege", "code_sp")) %>%
  mutate(AB = as.numeric(AB)) %>%
  select(3 : 5) 

Coleo1 <- Coleobis %>%
  filter(Num_piege == 1) %>%
  group_by(code_sp) %>%
  summarise(AB = sum(AB))
ggplot(Coleo1, aes(x = code_sp, y = AB)) +
  geom_bar(stat = "identity") +
  geom_text(aes(label = AB), vjust = -0.5, size = 3) +
  labs(title = "Abondance des espèces capturées dans les pièges 1 des arbres Thèses",
       x = "Pièges entomologiques",
       y = "Abondance (n)") +
  theme_minimal() +
  theme(plot.title = element_text(size = 9, face = "bold"),
        axis.text.x = element_text(angle = 0))

Coleo2 <- Coleobis %>%
  filter(Num_piege == 2) %>%
  group_by(code_sp) %>%
  summarise(AB = sum(AB))
ggplot(Coleo2, aes(x = code_sp, y = AB)) +
  geom_bar(stat = "identity") +
  geom_text(aes(label = AB), vjust = -0.5, size = 3) +
  labs(title = "Abondance des espèces capturées dans les pièges 1 des arbres Thèses",
       x = "Pièges entomologiques",
       y = "Abondance (n)") +
  theme_minimal() +
  theme(plot.title = element_text(size = 9, face = "bold"),
        axis.text.x = element_text(angle = 0))

###################################################################

```


# 4. Data loader

```{r}
  # BMP Strates
BMP_Strates <- read.csv("//no-datafile.inra.local/Biodiversite/projets/Bois_mort/Canopee/These_Balvay/Stagiaires/Adrien Debout - 03-02 au 01-08-2025/Data/Metriques_BMP_Strates_AD.csv", sep=";", header=TRUE, stringsAsFactors = FALSE)
  # BMP Arbres
BMP_Arbres <- read.csv("//no-datafile.inra.local/Biodiversite/projets/Bois_mort/Canopee/These_Balvay/Stagiaires/Adrien Debout - 03-02 au 01-08-2025/Data/Metriques_BMPtot_Arbres_AD.csv", sep=";", header=TRUE, stringsAsFactors = FALSE)
  # BMP Placettes
BMP_Placettes <- read.csv("//no-datafile.inra.local/Biodiversite/projets/Bois_mort/Canopee/These_Balvay/Stagiaires/Adrien Debout - 03-02 au 01-08-2025/Data/Metriques_BMPtot_Placette_AD.csv", sep=";", header=TRUE, stringsAsFactors = FALSE)
  # Data entomo
entomo <- read.table(here::here("Data", "Data placettes", "DATA_placette_CANOPIX", "metriques_CB", "metric_entomo2024.txt"), sep = "\t", dec = ".", quote = "\"", stringsAsFactors = F, header = T)
  # Data matrices CB
matrices <- read.table(here::here("Data", "Data placettes", "DATA_placette_CANOPIX", "metriques_CB", "matrix_spcolsx_pieges.txt"), sep = "\t", dec = ".", quote = "\"", stringsAsFactors = F, header = T)
dimnames(matrices) # attention 1 à 6 (7: 381)
  # Data longitude et latitude
GPS <- read.csv(here::here("Data", "Data placettes", "DATA_placette_CANOPIX", "metriques_CB", "info_arbre.csv"),
sep=",", header=TRUE, stringsAsFactors = FALSE)
colnames(GPS) <- "raw"
options(digits = 15)
GPS <- separate(GPS, raw, into = c("massif", "plot", "X_L93", "Y_L93", "X_WGS84", "Y_WGS84", "sp", "note_DEPERIS", "DBH.cm", "H_arbre.m", "id_arbre"), sep = ",", convert = TRUE)
options(digits = 15)

library(vegan)

distsupra<-vegdist(matrices[,7 : 381], method="bray",na.rm = TRUE)
adonis2(matrices[,7 : 381] ~ strate, matrices, permutations = 999, method = "bray",strata = LOCA)

library(emmeans)
emmeans()
?emmeans # glmmm peut expliqué attendre CB

# refaire TOUT les modèles en séparant Final par relevé 
# Réfléchir à intégrer relevé et strates dans modèles (strate * relevé)
# Partition de variance : approche multi-variable ( proposition des métriques dendro, qui influence signif le plus et qui sur l'abondance de coleo) avec VARPAR pour mesurer la proportion de variance mais également inertie 



###

# Modifier Jeu de données entomo pour créer une colonne placette #

  # Modification de entomo pour left_join avec les métriques X
entomo <- entomo %>%
  mutate(placette = paste0(substr(LOCA, 1, 1), "_", parcelle), .before = LOCA) %>%
  arrange(placette)
  # Passage en tableau long pour l'analyse du modèle
BMP_Strates <- BMP_Strates %>%
  mutate(Vol_BMP_AC.m3 = 0, .before = "Vol_BMP_UC.m3") %>% 
  # Rajout de la colonne AC pour avoir les 4 strates
  pivot_longer(cols = 2 : 5, names_to = "strates", values_to = "Volume_BMP.m3") %>%
  mutate(num.trap = case_when(strates == "Vol_BMP_AC.m3" ~ 1,
                             strates == "Vol_BMP_UC.m3" ~ 2,
                             strates == "Vol_BMP_LC.m3" ~ 3,
                             strates == "Vol_BMP_BC.m3" ~ 4)) %>%
  rename(placette = Placette) %>%
  arrange(placette)
  # Ligne de code pour ajouter les métriques X à entomo
  # Ajout de la localisation des placettes
GPS <- GPS %>%
  arrange(id_arbre) %>%
  rename(placette = id_arbre,
         latitude = Y_L93,
         longitude = X_L93) %>%
  select(3, 4, 11)
GPS$placette <- gsub('"', '', GPS$placette) # guillements en trop
GPS <- GPS %>%
  mutate(placette = ifelse(placette == "V_169", "V_170", placette)) 

Final <- BMP_Strates %>% 
  left_join(entomo, by = c("placette" = "placette", "num.trap" = "num.trap")) %>%
  left_join(GPS, by = "placette") %>%
  select(placette, longitude, latitude, everything())
#
Final <- Final %>%
  mutate(Volume_BMP.m3 = ifelse(Volume_BMP.m3 == 0, Volume_BMP.m3 + 0.000000001, Volume_BMP.m3),
         strates = case_when(strates == "Vol_BMP_AC.m3" ~ "AC",
                             strates == "Vol_BMP_UC.m3" ~ "UC",
                             strates == "Vol_BMP_LC.m3" ~ "LC",
                             strates == "Vol_BMP_BC.m3" ~ "BC")) %>%
  mutate(id = paste0(placette, "_", num.trap))
```

# 5. Analyses : effet strates
Différence de strates 
## 5.1. Ab_col_all

```{r}
descdist(entomo$ab.colsx,discrete=TRUE,boot=1001)
plot(fitdist(entomo$ab.colsx,"norm"))
#
fitnb<-fitdist(entomo$ab.colsx,"nbinom")
plot(fitdist(entomo$ab.colsx,"nbinom"))
#
fitp<-fitdist(entomo$ab.colsx,"pois")
plot(fitp)
#
fitn<-fitdist(entomo$ab.colsx,"norm")
plot(fitn) # oui
#
fitlnorm<-fitdist(entomo$ab.colsx,"lnorm")
plot(fitlnorm) # oui
#
fitexp<-fitdist(entomo$ab.colsx,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$ab.colsx,"geom")
plot(fitgeom)
#
fitbeta<-fitdist(entomo$ab.colsx,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$ab.colsx,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$ab.colsx,"gamma",method="mme")
plot(fitg) # oui

####

  # Modèle pour l'abondance, richesse spécifique
attach(Final)
longitude <- as.numeric(longitude) ; longitude
latitude <- as.numeric(latitude) ; latitude
strates <- as.factor(strates)
placette <- as.factor(placette)
id <- as.factor(id)
is.numeric(num.trap)
strate <- as.factor(strate)
pos<-numFactor(scale(longitude),scale(latitude)) ; pos
ID <- id ; ID
is.factor(strate)
is.numeric(ab.colsx)
is.numeric(num.trap)
num.trap <- as.factor (num.trap)
detach(Final)
# Vérifier si id.trap bien identifiant unique pour chaque ligne #
model_ABX_BMPS <- glmmTMB(
  ab.colsx ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)),
  family = "lognormal", 
  data = Final, 
  na.action = na.fail) ; summary(model_ABX_BMPS) # effet strate
 #
library(lme4)
?glmer
model_ABX_BMPS <- glmer(
  ab.colsx ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)) + exp(pos + 0 | ID),
  family = "gaussian"(link = "log"), 
  data = Final, 
  na.action = na.fail) ; summary(model_ABX_BMPS)
AICc(model_ABX_BMPS)
#
model_ABX_BMPS2 <- glmmTMB(
  ab.colsx ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "lognormal", 
  data = Final, 
  na.action = na.fail) ; summary(model_ABX_BMPS2)
#
AICc(model_ABX_BMPS2) # au minimum un delta de 2 pour l'ajustement des modèles
#
anova(model_ABX_BMPS, model_ABX_BMPS2) # 3.78e-06

####

#diagnostic glmm : ajustement, surdispersion, outliers
sim<-simulateResiduals(model_ABX_BMPS)
testUniformity(sim)#le qqplot est issu de cette commande
# id.trap si disersion (ex : (1 | LOCA / placette / id.trap)
# KS test : problème de distribution
#KS Test p-value # Dispersion test  # Outliers 
testOutliers(sim)

####

  # Test post-hoc de Tukey pour faire comparaison 2 à 2
RES<-glht(model_ABX_BMPS, linfct = mcp(strate = "Tukey"))
summary(RES)
0.0031270 **
# Est ce que supra diffère du reste : strate2 avec supra et autre canopée ou autre appellation (2 eme temps pas urgent)
```

## 5.2. Sr_col_all

```{r}
descdist(entomo$sr.colsx,discrete=TRUE,boot=1001)
plot(fitdist(entomo$sr.colsx,"norm")) # oui
#
fitnb<-fitdist(entomo$sr.colsx,"nbinom")
plot(fitdist(entomo$sr.colsx,"nbinom"))
#
fitp<-fitdist(entomo$sr.colsx,"pois")
plot(fitp)
#
fitn<-fitdist(entomo$sr.colsx,"norm")
plot(fitn) # oui
#
fitlnorm<-fitdist(entomo$sr.colsx,"lnorm")
plot(fitlnorm) # 
#
fitexp<-fitdist(entomo$sr.colsx,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$sr.colsx,"geom")
plot(fitgeom)
#
fitbeta<-fitdist(entomo$sr.colsx,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$sr.colsx,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$sr.colsx,"gamma",method="mme")
plot(fitg) # 

####

model_SRX_BMPS <- glmmTMB(
  sr.colsx ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "gaussian", 
  data = Final, 
  na.action = na.fail) ; summary(model_SRX_BMPS)
#
model_SRX2_BMPS <- glmmTMB(
  sr.colsx ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "gaussian", 
  data = Final, 
  na.action = na.fail) ; summary(model_SRX2_BMPS)
# comparaison des modèles
AICc(model_SRX_BMPS) # 636.4873
AICc(model_SRX2_BMPS) # 646.2442
#
anova(model_SRX_BMPS, model_SRX2_BMPS) # 0.0049384 **
####
#diagnostic glmm : ajustement, surdispersion, outliers
sim<-simulateResiduals(model_SRX_BMPS)
testUniformity(sim)#le qqplot est issu de cette commande 
#KS Test p-value # Dispersion test  # Outliers 
testOutliers(sim)

  # Test post-hoc de Tukey pour faire comparaison 2 à 2
RES<-glht(model_SRX_BMPS, linfct = mcp(strate = "Tukey"))
summary(RES)
# 0.0048236 **
```

## 5.3. Ab_col_rar

```{r}
descdist(entomo$ab.colsxrar,discrete=TRUE,boot=1001)
plot(fitdist(entomo$ab.colsxrar,"norm")) # oui
#
fitnb<-fitdist(entomo$ab.colsxrar,"nbinom")
plot(fitdist(entomo$ab.colsxrar,"nbinom"))
#
fitp<-fitdist(entomo$ab.colsxrar,"pois")
plot(fitp)
#
fitn<-fitdist(entomo$ab.colsxrar,"norm")
plot(fitn) # oui
#
fitlnorm<-fitdist(entomo$ab.colsxrar,"lnorm")
plot(fitlnorm) # 
#
fitexp<-fitdist(entomo$ab.colsxrar,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$ab.colsxrar,"geom")
plot(fitgeom)
#
fitbeta<-fitdist(entomo$ab.colsxrar,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$ab.colsxrar,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$ab.colsxrar,"gamma",method="mme")
plot(fitg) # 
#
modelABRAR <- glmmTMB(
  ab.colsxrar ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "gaussian", 
  data = Final, 
  na.action = na.fail) ; summary(modelABRAR)
#
modelABRAR2 <- glmmTMB(
  ab.colsxrar ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "gaussian", 
  data = Final, 
  na.action = na.fail) ; summary(modelABRAR2)
# comparaison des modèles
AICc(modelABRAR) # 620.6581
AICc(modelABRAR2) # 660.7834
#
anova(modelABRAR, modelABRAR2) # 3.2334e-10 ***
#
sim<-simulateResiduals(modelABRAR)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelABRAR, linfct = mcp(strate = "Tukey"))
summary(RES) # < 0.001 ***

```

## 5.4. Sr_col_rar

```{r}
descdist(entomo$sr.colsxrar,discrete=TRUE,boot=1001)
plot(fitdist(entomo$sr.colsxrar,"norm")) #
#
?glmmTMB
fitnb<-fitdist(entomo$sr.colsxrar,"nbinom")
plot(fitdist(entomo$sr.colsxrar,"nbinom")) # oui
# 
fitp<-fitdist(entomo$sr.colsxrar,"pois")
plot(fitp) # oui
#
fitn<-fitdist(entomo$sr.colsxrar,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$sr.colsxrar,"lnorm")
plot(fitlnorm) # 
#
fitexp<-fitdist(entomo$sr.colsxrar,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$sr.colsxrar,"geom")
plot(fitgeom)
#
fitbeta<-fitdist(entomo$sr.colsxrar,"beta") # 
plot(fitbeta)
#
fitunif<-fitdist(entomo$sr.colsxrar,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$sr.colsxrar,"gamma",method="mme")
plot(fitg) # 
#
?glmmTMB
modelSRRAR <- glmmTMB(
  sr.colsxrar ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "genpois", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRRAR)
#
modelSRRAR2 <- glmmTMB(
  sr.colsxrar ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "genpois", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRRAR2)
#
AICc(modelSRRAR) # 330.9763
AICc(modelSRRAR2) # 374.6217
#
anova(modelSRRAR, modelSRRAR2) # 5.759e-11 ***
#
sim<-simulateResiduals(modelSRRAR)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelSRRAR, linfct = mcp(strate = "Tukey"))
summary(RES) # 0.00511 ** 
```

## 5.5. Ab_col_com

```{r}
descdist(entomo$ab.colsxcom,discrete=TRUE,boot=1001)
plot(fitdist(entomo$ab.colsxcom,"norm")) #
#
fitnb<-fitdist(entomo$ab.colsxcom,"nbinom")
plot(fitdist(entomo$ab.colsxcom,"nbinom")) # 
# 
fitp<-fitdist(entomo$ab.colsxcom,"pois")
plot(fitp) # 
#
fitn<-fitdist(entomo$ab.colsxcom,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$ab.colsxcom,"lnorm")
plot(fitlnorm) # oui
#
fitexp<-fitdist(entomo$ab.colsxcom,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$ab.colsxcom,"geom")
plot(fitgeom)
#
fitbeta<-fitdist(entomo$ab.colsxcom,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$ab.colsxcom,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$ab.colsxcom,"gamma",method="mme")
plot(fitg) # oui
#
modelABCOM <- glmmTMB(
  ab.colsxcom ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "lognormal", 
  data = Final, 
  na.action = na.fail) ; summary(modelABCOM)
#
modelABCOM2 <- glmmTMB(
  ab.colsxcom ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "lognormal", 
  data = Final, 
  na.action = na.fail) ; summary(modelABCOM2)
#
AICc(modelABCOM) # 930.4896
AICc(modelABCOM2) # 949.2164
#
anova(modelABCOM, modelABCOM2) # 1.076e-05 ***
#
sim<-simulateResiduals(modelABCOM)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelABCOM, linfct = mcp(strate = "Tukey"))
summary(RES) # 0.01575 * 
```

## 5.6. Sr_col_com

```{r}
descdist(entomo$sr.colsxcom,discrete=TRUE,boot=1001)
plot(fitdist(entomo$sr.colsxcom,"norm")) #
#
fitnb<-fitdist(entomo$sr.colsxcom,"nbinom")
plot(fitdist(entomo$sr.colsxcom,"nbinom")) # oui
# 
fitp<-fitdist(entomo$sr.colsxcom,"pois")
plot(fitp) # 
#
fitn<-fitdist(entomo$sr.colsxcom,"norm")
plot(fitn) # oui
#
fitlnorm<-fitdist(entomo$sr.colsxcom,"lnorm")
plot(fitlnorm) # 
#
fitexp<-fitdist(entomo$sr.colsxcom,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$sr.colsxcom,"geom")
plot(fitgeom)
#
fitbeta<-fitdist(entomo$sr.colsxcom,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$sr.colsxcom,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$sr.colsxcom,"gamma",method="mme")
plot(fitg) # 
#
modelSRCOM <- glmmTMB(
  sr.colsxcom ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "gaussian", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRCOM)
#
modelSRCOM2 <- glmmTMB(
  sr.colsxcom ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "gaussian", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRCOM2)
#
AICc(modelSRCOM) # 614.1401
AICc(modelSRCOM2) # 618.5605
#
anova(modelSRCOM, modelSRCOM2) # 0.009558 **
#
sim<-simulateResiduals(modelSRCOM)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelSRCOM, linfct = mcp(strate = "Tukey"))
summary(RES) # 0.0079 ** 
```

## 5.7. ab_cav

```{r}
descdist(entomo$ab.cav,discrete=TRUE,boot=1001)
plot(fitdist(entomo$ab.cav,"norm")) #
#
fitnb<-fitdist(entomo$ab.cav,"nbinom")
plot(fitdist(entomo$ab.cav,"nbinom")) # 
# 
fitp<-fitdist(entomo$ab.cav,"pois")
plot(fitp) # 
#
fitn<-fitdist(entomo$ab.cav,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$ab.cav,"lnorm")
plot(fitlnorm) # oui
#
fitexp<-fitdist(entomo$ab.cav,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$ab.cav,"geom")
plot(fitgeom)
#
fitbeta<-fitdist(entomo$ab.cav,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$ab.cav,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$ab.cav,"gamma",method="mme")
plot(fitg) # oui
#
modelABCAV <- glmmTMB(
  ab.cav ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "lognormal", 
  data = Final, 
  na.action = na.fail) ; summary(modelABCAV)
#
modelABCAV2 <- glmmTMB(
  ab.cav ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "lognormal", 
  data = Final, 
  na.action = na.fail) ; summary(modelABCAV2)
#
AICc(modelABCAV) # 685.2339
AICc(modelABCAV2) # 702.1384
#
anova(modelABCAV,modelABCAV2) # 2.588e-05 ***
#
sim<-simulateResiduals(modelABCAV)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelABCAV, linfct = mcp(strate = "Tukey"))
summary(RES) # 0.0079 ** 
```

## 5.8. sr_cav

```{r}
descdist(entomo$sr.cav,discrete=TRUE,boot=1001)
plot(fitdist(entomo$sr.cav,"norm")) #
#
fitnb<-fitdist(entomo$sr.cav,"nbinom")
plot(fitdist(entomo$sr.cav,"nbinom")) # oui
# 
fitp<-fitdist(entomo$sr.cav,"pois")
plot(fitp) # oui
#
fitn<-fitdist(entomo$sr.cav,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$sr.cav,"lnorm")
plot(fitlnorm) # 
#
fitexp<-fitdist(entomo$sr.cav,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$sr.cav,"geom")
plot(fitgeom)
#
fitbeta<-fitdist(entomo$sr.cav,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$sr.cav,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$sr.cav,"gamma",method="mme")
plot(fitg) # 
#
modelSRCAV <- glmmTMB(
  sr.cav ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "genpois", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRCAV)
#
modelSRCAV2 <- glmmTMB(
  sr.cav ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "genpois", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRCAV2)
#
AICc(modelSRCAV) # 453.7887
AICc(modelSRCAV2) # 467.9944
#
anova(modelSRCAV,modelSRCAV2) # 9.44e-05 ***
#
sim<-simulateResiduals(modelSRCAV)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelABCAV, linfct = mcp(strate = "Tukey"))
summary(RES) # 0.0079 ** 
```

## 5.9. ab_fon

```{r}
descdist(entomo$ab.fon,discrete=TRUE,boot=1001)
plot(fitdist(entomo$ab.fon,"norm")) #
#
fitnb<-fitdist(entomo$ab.fon,"nbinom")
plot(fitdist(entomo$ab.fon,"nbinom")) # 
# 
fitp<-fitdist(entomo$ab.fon,"pois")
plot(fitp) # 
#
fitn<-fitdist(entomo$ab.fon,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$ab.fon,"lnorm")
plot(fitlnorm) # oui
#
fitexp<-fitdist(entomo$ab.fon,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$ab.fon,"geom")
plot(fitgeom)
#
fitbeta<-fitdist(entomo$ab.fon,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$ab.fon,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$ab.fon,"gamma",method="mme")
plot(fitg) # 
#
modelABFON <- glmmTMB(
  ab.fon ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "lognormal", 
  data = Final, 
  na.action = na.fail) ; summary(modelABFON)
#
modelABFON2 <- glmmTMB(
  ab.fon ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "lognormal", 
  data = Final, 
  na.action = na.fail) ; summary(modelABFON2)
#
AICc(modelABFON) # 861.6836
AICc(modelABFON2) # 863.6748
#
anova(modelABFON,modelABFON2) # 0.02911 *
#
sim<-simulateResiduals(modelABFON)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelABFON, linfct = mcp(strate = "Tukey"))
summary(RES) # 0.0079 ** 
```

## 5.10. sr_fon

```{r}
descdist(entomo$sr.fon,discrete=TRUE,boot=1001)
plot(fitdist(entomo$sr.fon,"norm")) #
#
fitnb<-fitdist(entomo$sr.fon,"nbinom")
plot(fitdist(entomo$sr.fon,"nbinom")) # 
# 
fitp<-fitdist(entomo$sr.fon,"pois")
plot(fitp) # oui
#
fitn<-fitdist(entomo$sr.fon,"norm")
plot(fitn) # oui
#
fitlnorm<-fitdist(entomo$sr.fon,"lnorm")
plot(fitlnorm) # 
#
fitexp<-fitdist(entomo$sr.fon,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$sr.fon,"geom")
plot(fitgeom)
#
fitbeta<-fitdist(entomo$sr.fon,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$sr.fon,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$sr.fon,"gamma",method="mme")
plot(fitg) # 
#
modelSRFON <- glmmTMB(
  sr.fon ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "genpois", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRFON)
#
modelSRFON2 <- glmmTMB(
  sr.fon ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "genpois", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRFON2)
#
AICc(modelSRFON) # 470.2642
AICc(modelSRFON2) # 478.399
#
anova(modelSRFON,modelSRFON2) # 0.001687 **
#
sim<-simulateResiduals(modelSRFON)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelSRFON, linfct = mcp(strate = "Tukey"))
summary(RES) # 0.0247 * 
```

## 5.11. ab_fon_spe

```{r}
descdist(entomo$ab.fonspe,discrete=TRUE,boot=1001)
plot(fitdist(entomo$ab.fonspe,"norm")) #
#
fitnb<-fitdist(entomo$ab.fonspe,"nbinom")
plot(fitdist(entomo$ab.fonspe,"nbinom")) # oui
# 
fitp<-fitdist(entomo$ab.fonspe,"pois")
plot(fitp) # 
#
fitn<-fitdist(entomo$ab.fonspe,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$ab.fonspe,"lnorm")
plot(fitlnorm) # 
#
fitexp<-fitdist(entomo$ab.fonspe,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$ab.fonspe,"geom")
plot(fitgeom)
#
fitbeta<-fitdist(entomo$ab.fonspe,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$ab.fonspe,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$ab.fonspe,"gamma",method="mme")
plot(fitg) # 
#
modelABFONspe <- glmmTMB(
  ab.fonspe ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "nbinom1", 
  data = Final, 
  na.action = na.fail) ; summary(modelABFONspe)
#
modelABFON2spe <- glmmTMB(
  ab.fonspe ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "lognormal", 
  data = Final, 
  na.action = na.fail) ; summary(modelABFON2spe)
#
AICc(modelABFONspe) # 484.316
AICc(modelABFON2spe) # 495.8483
#
anova(modelABFONspe,modelABFON2spe) # 0.000338 ***
#
sim<-simulateResiduals(modelABFONspe)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelABFONspe, linfct = mcp(strate = "Tukey"))
summary(RES) # 0.00227 ** 
```

## 5.12. sr_fon_spe

```{r}
descdist(entomo$sr.fonspe,discrete=TRUE,boot=1001)
plot(fitdist(entomo$sr.fonspe,"norm")) #
#
fitnb<-fitdist(entomo$sr.fonspe,"nbinom")
plot(fitdist(entomo$sr.fonspe,"nbinom")) # oui
# 
fitp<-fitdist(entomo$sr.fonspe,"pois")
plot(fitp) # ou
#
fitn<-fitdist(entomo$sr.fonspe,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$sr.fonspe,"lnorm")
plot(fitlnorm) # 
#
fitexp<-fitdist(entomo$sr.fonspe,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$sr.fonspe,"geom")
plot(fitgeom)
#
fitbeta<-fitdist(entomo$sr.fonspe,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$sr.fonspe,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$sr.fonspe,"gamma",method="mme")
plot(fitg) # 
#
modelSRFONspe <- glmmTMB(
  sr.fonspe ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "genpois", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRFONspe)
#
modelSRFON2spe <- glmmTMB(
  sr.fonspe ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "genpois", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRFON2spe)
#
AICc(modelSRFONspe) # 315.5848
AICc(modelSRFON2spe) # 319.8235
#
anova(modelSRFONspe,modelSRFON2spe) # 0.0104 *
#
sim<-simulateResiduals(modelSRFONspe)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelSRFONspe, linfct = mcp(strate = "Tukey"))
summary(RES) # 0.00293 ** 
```

## 5.13. ab_suc

```{r}
descdist(entomo$ab.suc,discrete=TRUE,boot=1001)
plot(fitdist(entomo$ab.suc,"norm")) #
#
fitnb<-fitdist(entomo$ab.suc,"nbinom")
plot(fitdist(entomo$ab.suc,"nbinom")) # oui
# 
fitp<-fitdist(entomo$ab.suc,"pois")
plot(fitp) # 
#
fitn<-fitdist(entomo$ab.suc,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$ab.suc,"lnorm")
plot(fitlnorm) # 
#
fitexp<-fitdist(entomo$ab.suc,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$ab.suc,"geom")
plot(fitgeom) # oui
#
fitbeta<-fitdist(entomo$ab.suc,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$ab.suc,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$ab.suc,"gamma",method="mme")
plot(fitg) # 
#
modelABSUC <- glmmTMB(
  ab.suc ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "nbinom1", 
  data = Final, 
  na.action = na.fail) ; summary(modelABSUC)
#
modelABSUC2 <- glmmTMB(
  ab.suc ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "nbinom1", 
  data = Final, 
  na.action = na.fail) ; summary(modelABSUC2)
#
AICc(modelABSUC) # 371.9953
AICc(modelABSUC2) # 382.6358
#
anova(modelABSUC,modelABSUC2) # 0.0005162 ***
#
sim<-simulateResiduals(modelABSUC)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelABSUC, linfct = mcp(strate = "Tukey"))
summary(RES) # <0.001 *** 
```

## 5.14. sr_suc

```{r}
descdist(entomo$sr.suc,discrete=TRUE,boot=1001)
plot(fitdist(entomo$sr.suc,"norm")) #
#
fitnb<-fitdist(entomo$sr.suc,"nbinom")
plot(fitdist(entomo$sr.suc,"nbinom")) # oui
# 
fitp<-fitdist(entomo$sr.suc,"pois")
plot(fitp) # oui
#
fitn<-fitdist(entomo$sr.suc,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$sr.suc,"lnorm")
plot(fitlnorm) # 
#
fitexp<-fitdist(entomo$sr.suc,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$sr.suc,"geom")
plot(fitgeom) # 
#
fitbeta<-fitdist(entomo$sr.suc,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$sr.suc,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$sr.suc,"gamma",method="mme")
plot(fitg) # 
#
modelSRSUC <- glmmTMB(
  sr.suc ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "genpois", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRSUC)
#
modelSRSUC2 <- glmmTMB(
  sr.suc ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "genpois", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRSUC2)
#
AICc(modelSRSUC) # 266.49
AICc(modelSRSUC2) # 267.4356
#
anova(modelSRSUC,modelSRSUC2) # 0.04668 *
#
sim<-simulateResiduals(modelSRSUC2)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelSRSUC2, linfct = mcp(strate = "Tukey"))
summary(RES) # <0.001 *** 
```

## 5.15. ab_lig

```{r}
descdist(entomo$ab.lig,discrete=TRUE,boot=1001)
plot(fitdist(entomo$ab.lig,"norm")) #
#
fitnb<-fitdist(entomo$ab.lig,"nbinom")
plot(fitdist(entomo$ab.lig,"nbinom")) # 
# 
fitp<-fitdist(entomo$ab.lig,"pois")
plot(fitp) # 
#
fitn<-fitdist(entomo$ab.lig,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$ab.lig,"lnorm")
plot(fitlnorm) # oui
#
fitexp<-fitdist(entomo$ab.lig,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$ab.lig,"geom")
plot(fitgeom) # 
#
fitbeta<-fitdist(entomo$ab.lig,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$ab.lig,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$ab.lig,"gamma",method="mme")
plot(fitg) # 
#
modelABLIG <- glmmTMB(
  ab.lig ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "lognormal", 
  data = Final, 
  na.action = na.fail) ; summary(modelABLIG)
#
modelABLIG2 <- glmmTMB(
  ab.lig ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "lognormal", 
  data = Final, 
  na.action = na.fail) ; summary(modelABLIG2)
#
AICc(modelABLIG) # 949.4589
AICc(modelABLIG2) # 969.5341
#
anova(modelABLIG,modelABLIG2) # 5.617e-06 ***
#
sim<-simulateResiduals(modelABLIG)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelABLIG, linfct = mcp(strate = "Tukey"))
summary(RES) # 0.00322 **
```

## 5.16. sr_lig

```{r}
descdist(entomo$sr.lig,discrete=TRUE,boot=1001)
plot(fitdist(entomo$sr.lig,"norm")) #
#
fitnb<-fitdist(entomo$sr.lig,"nbinom")
plot(fitdist(entomo$sr.lig,"nbinom")) # 
# 
fitp<-fitdist(entomo$sr.lig,"pois")
plot(fitp) # 
#
fitn<-fitdist(entomo$sr.lig,"norm")
plot(fitn) # oui
#
fitlnorm<-fitdist(entomo$sr.lig,"lnorm")
plot(fitlnorm) # oui
#
fitexp<-fitdist(entomo$sr.lig,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$sr.lig,"geom")
plot(fitgeom) # 
#
fitbeta<-fitdist(entomo$sr.lig,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$sr.lig,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$sr.lig,"gamma",method="mme")
plot(fitg) # oui
#
modelSRLIG <- glmmTMB(
  sr.lig ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "gaussian", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRLIG)
#
modelSRLIG2 <- glmmTMB(
  sr.lig ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "gaussian", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRLIG2)
#
AICc(modelSRLIG) # 620.5829
AICc(modelSRLIG2) # 634.0627
#
anova(modelSRLIG,modelSRLIG2) # 0.0001336 ***
#
sim<-simulateResiduals(modelSRLIG)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelSRLIG, linfct = mcp(strate = "Tukey"))
summary(RES) # <0.001 ***
```

## 5.17. ab_flo

```{r}
descdist(entomo$ab.flo,discrete=TRUE,boot=1001)
plot(fitdist(entomo$ab.flo,"norm")) #
#
fitnb<-fitdist(entomo$ab.flo,"nbinom")
plot(fitdist(entomo$ab.flo,"nbinom")) # 
# 
fitp<-fitdist(entomo$ab.flo,"pois")
plot(fitp) # 
#
fitn<-fitdist(entomo$ab.flo,"norm")
plot(fitn) # oui
#
fitlnorm<-fitdist(entomo$ab.flo,"lnorm")
plot(fitlnorm) # oui
#
fitexp<-fitdist(entomo$ab.flo,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$ab.flo,"geom")
plot(fitgeom) # 
#
fitbeta<-fitdist(entomo$ab.flo,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$ab.flo,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$ab.flo,"gamma",method="mme")
plot(fitg) # 
#
modelABFLO <- glmmTMB(
  ab.flo ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "lognormal", 
  data = Final, 
  na.action = na.fail) ; summary(modelABFLO)
#
modelABFLO2 <- glmmTMB(
  ab.flo ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "lognormal", 
  data = Final, 
  na.action = na.fail) ; summary(modelABFLO2)
#
AICc(modelABFLO) # 745.4168
AICc(modelABFLO2) # 790.4968
#
anova(modelABFLO,modelABFLO2) # 2.849e-11 ***
#
sim<-simulateResiduals(modelABFLO)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelABFLO, linfct = mcp(strate = "Tukey"))
summary(RES) # 0.00146 **
```

## 5.18. sr_flo

```{r}
descdist(entomo$sr.flo,discrete=TRUE,boot=1001)
plot(fitdist(entomo$sr.flo,"norm")) #
#
fitnb<-fitdist(entomo$sr.flo,"nbinom")
plot(fitdist(entomo$sr.flo,"nbinom")) # oui 
# 
fitp<-fitdist(entomo$sr.flo,"pois")
plot(fitp) # oui
#
fitn<-fitdist(entomo$sr.flo,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$sr.flo,"lnorm")
plot(fitlnorm) # 
#
fitexp<-fitdist(entomo$sr.flo,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$sr.flo,"geom")
plot(fitgeom) # 
#
fitbeta<-fitdist(entomo$sr.flo,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$sr.flo,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$sr.flo,"gamma",method="mme")
plot(fitg) # 
#
modelSRFLO <- glmmTMB(
  sr.flo ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "nbinom2", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRFLO)
#
modelSRFLO2 <- glmmTMB(
  sr.flo ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "nbinom2", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRFLO2)
#
AICc(modelSRFLO) # 466.81
AICc(modelSRFLO2) # 500.5577
#
anova(modelSRFLO,modelSRFLO2) # 7.317e-09 ***
#
sim<-simulateResiduals(modelSRFLO)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelSRFLO, linfct = mcp(strate = "Tukey"))
summary(RES) # < 0.001 ***
```

## 5.19. ab_pho

```{r}
descdist(entomo$ab.pho,discrete=TRUE,boot=1001)
plot(fitdist(entomo$ab.pho,"norm")) #
#
fitnb<-fitdist(entomo$ab.pho,"nbinom")
plot(fitdist(entomo$ab.pho,"nbinom")) #  oui
# 
fitp<-fitdist(entomo$ab.pho,"pois")
plot(fitp) # 
#
fitn<-fitdist(entomo$ab.pho,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$ab.pho,"lnorm")
plot(fitlnorm) # 
#
fitexp<-fitdist(entomo$ab.pho,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$ab.pho,"geom")
plot(fitgeom) # 
#
fitbeta<-fitdist(entomo$ab.pho,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$ab.pho,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$ab.pho,"gamma",method="mme")
plot(fitg) # 
#
modelABPHO <- glmmTMB(
  ab.pho ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "nbinom1", 
  data = Final, 
  na.action = na.fail) ; summary(modelABPHO)
#
modelABPHO2 <- glmmTMB(
  ab.pho ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "nbinom1", 
  data = Final, 
  na.action = na.fail) ; summary(modelABPHO2)
#
AICc(modelABPHO) # 450.869
AICc(modelABPHO2) # 459.3375
#
anova(modelABPHO,modelABPHO2) # 0.001442 **
#
sim<-simulateResiduals(modelABPHO)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelABPHO, linfct = mcp(strate = "Tukey"))
summary(RES) # 0.00712 ** 
```

## 5.20. sr_pho

```{r}
descdist(entomo$sr.pho,discrete=TRUE,boot=1001)
plot(fitdist(entomo$sr.pho,"norm")) #
#
fitnb<-fitdist(entomo$sr.pho,"nbinom")
plot(fitdist(entomo$sr.pho,"nbinom")) #  oui
# 
fitp<-fitdist(entomo$sr.pho,"pois")
plot(fitp) # oui
#
fitn<-fitdist(entomo$sr.pho,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$sr.pho,"lnorm")
plot(fitlnorm) # 
#
fitexp<-fitdist(entomo$sr.pho,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$sr.pho,"geom")
plot(fitgeom) # 
#
fitbeta<-fitdist(entomo$sr.pho,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$sr.pho,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$sr.pho,"gamma",method="mme")
plot(fitg) # 
#
modelSRPHO <- glmmTMB(
  sr.pho ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "genpois", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRPHO)
#
modelSRPHO2 <- glmmTMB(
  sr.pho ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "genpois", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRPHO2)
#
AICc(modelSRPHO) # 262.2215
AICc(modelSRPHO2) # 272.1756
#
anova(modelSRPHO,modelSRPHO2) # 0.0007147 ***
#
sim<-simulateResiduals(modelSRPHO)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelSRPHO, linfct = mcp(strate = "Tukey"))
summary(RES) # 0.00347 **
```

## 5.21. ab_myc

```{r}
descdist(entomo$ab.myc,discrete=TRUE,boot=1001)
plot(fitdist(entomo$ab.myc,"norm")) #
#
fitnb<-fitdist(entomo$ab.myc,"nbinom")
plot(fitdist(entomo$ab.myc,"nbinom")) #  oui
# 
fitp<-fitdist(entomo$ab.myc,"pois")
plot(fitp) # oui
#
fitn<-fitdist(entomo$ab.myc,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$ab.myc,"lnorm")
plot(fitlnorm) # oui
#
fitexp<-fitdist(entomo$ab.myc,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$ab.myc,"geom")
plot(fitgeom) # 
#
fitbeta<-fitdist(entomo$ab.myc,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$ab.myc,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$ab.myc,"gamma",method="mme")
plot(fitg) # 
#
modelABMYC <- glmmTMB(
  ab.myc ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "lognormal", 
  data = Final, 
  na.action = na.fail) ; summary(modelABMYC)
#
modelABMYC2 <- glmmTMB(
  ab.myc ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "lognormal", 
  data = Final, 
  na.action = na.fail) ; summary(modelABMYC2)
#
AICc(modelABMYC) # 753.5138
AICc(modelABMYC2) # 761.3229
#
anova(modelABMYC,modelABMYC2) # 0.001967 **
#
sim<-simulateResiduals(modelABMYC)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelABMYC, linfct = mcp(strate = "Tukey"))
summary(RES) # 0.0014 **
```

## 5.22. sr_myc

```{r}
descdist(entomo$sr.myc,discrete=TRUE,boot=1001)
plot(fitdist(entomo$sr.myc,"norm")) #
#
fitnb<-fitdist(entomo$sr.myc,"nbinom")
plot(fitdist(entomo$sr.myc,"nbinom")) #  oui
# 
fitp<-fitdist(entomo$sr.myc,"pois")
plot(fitp) # oui
#
fitn<-fitdist(entomo$sr.myc,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$sr.myc,"lnorm")
plot(fitlnorm) # oui
#
fitexp<-fitdist(entomo$sr.myc,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$sr.myc,"geom")
plot(fitgeom) # 
#
fitbeta<-fitdist(entomo$sr.myc,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$sr.myc,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$sr.myc,"gamma",method="mme")
plot(fitg) # 
#
modelSRMYC <- glmmTMB(
  sr.myc ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "genpois", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRMYC)
#
modelSRMYC2 <- glmmTMB(
  sr.myc ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "genpois", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRMYC2)
#
AICc(modelSRMYC) # 399.8433
AICc(modelSRMYC2) # 401.8182
#
anova(modelSRMYC,modelSRMYC2) # 0.02933 *
#
sim<-simulateResiduals(modelSRMYC)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelSRMYC, linfct = mcp(strate = "Tukey"))
summary(RES) # 0.0014 **
```

## 5.23. ab_xyl

```{r}
descdist(entomo$ab.xyl,discrete=TRUE,boot=1001)
plot(fitdist(entomo$ab.xyl,"norm")) #
#
fitnb<-fitdist(entomo$ab.xyl,"nbinom")
plot(fitdist(entomo$ab.xyl,"nbinom")) #  
# 
fitp<-fitdist(entomo$ab.xyl,"pois")
plot(fitp) # 
#
fitn<-fitdist(entomo$ab.xyl,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$ab.xyl,"lnorm")
plot(fitlnorm) # oui
#
fitexp<-fitdist(entomo$ab.xyl,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$ab.xyl,"geom")
plot(fitgeom) # oui
#
fitbeta<-fitdist(entomo$ab.xyl,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$ab.xyl,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$ab.xyl,"gamma",method="mme")
plot(fitg) # 
#
modelABXYL <- glmmTMB(
  ab.xyl ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "nbinom2",
  data = Final, 
  na.action = na.fail) ; summary(modelABXYL)
#
modelABXYL2 <- glmmTMB(
  ab.xyl ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "nbinom2",
  data = Final, 
  na.action = na.fail) ; summary(modelABXYL2)
#
AICc(modelABXYL) # 797.8552
AICc(modelABXYL2) # 814.7558
#
anova(modelABXYL,modelABXYL2) # 2.592e-05 ***
#
sim<-simulateResiduals(modelABXYL)
plot(sim)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelABXYL, linfct = mcp(strate = "Tukey"))
summary(RES) # 0.00771 **
```

## 5.24. sr_xyl

```{r}
descdist(entomo$sr.xyl,discrete=TRUE,boot=1001)
plot(fitdist(entomo$sr.xyl,"norm")) #
#
fitnb<-fitdist(entomo$sr.xyl,"nbinom")
plot(fitdist(entomo$sr.xyl,"nbinom")) #  oui
# 
fitp<-fitdist(entomo$sr.xyl,"pois")
plot(fitp) # oui
#
fitn<-fitdist(entomo$sr.xyl,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$sr.xyl,"lnorm")
plot(fitlnorm) # 
#
fitexp<-fitdist(entomo$sr.xyl,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$sr.xyl,"geom")
plot(fitgeom) # 
#
fitbeta<-fitdist(entomo$sr.xyl,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$sr.xyl,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$sr.xyl,"gamma",method="mme")
plot(fitg) # 
#
modelSRXYL <- glmmTMB(
  sr.xyl ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "truncated_genpois", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRXYL)
#
modelSRXYL2 <- glmmTMB(
  sr.xyl ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "truncated_genpois", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRXYL2)
#
AICc(modelSRXYL) # 422.2892
AICc(modelSRXYL2) # 466.4268
#
anova(modelSRXYL,modelSRXYL2) # 4.523e-11 ***
#
sim<-simulateResiduals(modelSRXYL)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelSRXYL, linfct = mcp(strate = "Tukey"))
summary(RES) # < 1e-04 ***
```

## 5.25. ab_sxy

```{r}
descdist(entomo$ab.sxy,discrete=TRUE,boot=1001)
plot(fitdist(entomo$ab.sxy,"norm")) #
#
fitnb<-fitdist(entomo$ab.sxy,"nbinom")
plot(fitdist(entomo$ab.sxy,"nbinom")) #  
# 
fitp<-fitdist(entomo$ab.sxy,"pois")
plot(fitp) # 
#
fitn<-fitdist(entomo$ab.sxy,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$ab.sxy,"lnorm")
plot(fitlnorm) # oui
#
fitexp<-fitdist(entomo$ab.sxy,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$ab.sxy,"geom")
plot(fitgeom) # 
#
fitbeta<-fitdist(entomo$ab.sxy,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$ab.sxy,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$ab.sxy,"gamma",method="mme")
plot(fitg) # 
#
modelABSXY <- glmmTMB(
  ab.sxy ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = lognormal(link = "identity"), 
  data = Final, 
  na.action = na.fail) ; summary(modelABSXY)
#
modelABSXY2 <- glmmTMB(
  ab.sxy ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = lognormal(link = "identity"), 
  data = Final, 
  na.action = na.fail) ; summary(modelABSXY2)
#
AICc(modelABSXY) # 725.9916
AICc(modelABSXY2) # 736.1654
#
anova(modelABSXY,modelABSXY2) # 0.0006441 ***
#
sim<-simulateResiduals(modelABSXY)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelABSXY, linfct = mcp(strate = "Tukey"))
summary(RES) # 0.00659 **
```

## 5.26. sr_sxy

```{r}
descdist(entomo$sr.sxy,discrete=TRUE,boot=1001)
plot(fitdist(entomo$sr.sxy,"norm")) #
#
fitnb<-fitdist(entomo$sr.sxy,"nbinom")
plot(fitdist(entomo$sr.sxy,"nbinom")) # oui
# 
fitp<-fitdist(entomo$sr.sxy,"pois")
plot(fitp) # 
#
fitn<-fitdist(entomo$sr.sxy,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$sr.sxy,"lnorm")
plot(fitlnorm) # 
#
fitexp<-fitdist(entomo$sr.sxy,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$sr.sxy,"geom")
plot(fitgeom) # 
#
fitbeta<-fitdist(entomo$sr.sxy,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$sr.sxy,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$sr.sxy,"gamma",method="mme")
plot(fitg) # 
#
modelSRSXY <- glmmTMB(
  sr.sxy ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "nbinom1", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRSXY)
#
modelSRSXY2 <- glmmTMB(
  sr.sxy ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "nbinom1", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRSXY2)
#
AICc(modelSRSXY) # 467.2723
AICc(modelSRSXY2) # 491.2692
#
anova(modelSRSXY,modelSRSXY2) # 8.422e-07 ***
#
sim<-simulateResiduals(modelSRSXY)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelSRSXY, linfct = mcp(strate = "Tukey"))
summary(RES) # < 0.001 ***
```

## 5.27. ab_zoo

```{r}
descdist(entomo$ab.zoo,discrete=TRUE,boot=1001)
plot(fitdist(entomo$ab.zoo,"norm")) #
#
fitnb<-fitdist(entomo$ab.zoo,"nbinom")
plot(fitdist(entomo$ab.zoo,"nbinom")) # 
# 
fitp<-fitdist(entomo$ab.zoo,"pois")
plot(fitp) # 
#
fitn<-fitdist(entomo$ab.zoo,"norm")
plot(fitn) # oui
#
fitlnorm<-fitdist(entomo$ab.zoo,"lnorm")
plot(fitlnorm) # oui
#
fitexp<-fitdist(entomo$ab.zoo,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$ab.zoo,"geom")
plot(fitgeom) # 
#
fitbeta<-fitdist(entomo$ab.zoo,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$ab.zoo,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$ab.zoo,"gamma",method="mme")
plot(fitg) # 
#
modelABZOO <- glmmTMB(
  ab.zoo ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = gaussian(link = "log"), 
  data = Final, 
  na.action = na.fail) ; summary(modelABZOO)
#
modelABZOO2 <- glmmTMB(
  ab.zoo ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = gaussian(link = "log"), 
  data = Final, 
  na.action = na.fail) ; summary(modelABZOO2)
#
AICc(modelABZOO) # 725.9477
AICc(modelABZOO2) # 763.8545
#
anova(modelABZOO,modelABZOO2) # 9.58e-10 ***
#
sim<-simulateResiduals(modelABZOO)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelABZOO, linfct = mcp(strate = "Tukey"))
summary(RES) # 0.00482 **
```

## 5.28. sr_zoo

```{r}
descdist(entomo$sr.zoo,discrete=TRUE,boot=1001)
plot(fitdist(entomo$sr.zoo,"norm")) #
#
fitnb<-fitdist(entomo$sr.zoo,"nbinom")
plot(fitdist(entomo$sr.zoo,"nbinom")) # oui
# 
fitp<-fitdist(entomo$sr.zoo,"pois")
plot(fitp) # oui
#
fitn<-fitdist(entomo$sr.zoo,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$sr.zoo,"lnorm")
plot(fitlnorm) # 
#
fitexp<-fitdist(entomo$sr.zoo,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$sr.zoo,"geom")
plot(fitgeom) # 
#
fitbeta<-fitdist(entomo$sr.zoo,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$sr.zoo,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$sr.zoo,"gamma",method="mme")
plot(fitg) # 
#
modelSRZOO <- glmmTMB(
  sr.zoo ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "nbinom1", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRZOO)
#
modelSRZOO2 <- glmmTMB(
  sr.zoo ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "nbinom1", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRZOO2)
#
AICc(modelSRZOO) # 431.6754
AICc(modelSRZOO2) # 451.1683
#
anova(modelSRZOO,modelSRZOO2) # 7.44e-06 ***
#
sim<-simulateResiduals(modelSRZOO)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelSRZOO, linfct = mcp(strate = "Tukey"))
summary(RES) # 0.00184 ** 
```

## 5.29. ab_cavspe

```{r}
descdist(entomo$ab.cavspe,discrete=TRUE,boot=1001)
plot(fitdist(entomo$ab.cavspe,"norm")) #
#
fitnb<-fitdist(entomo$ab.cavspe,"nbinom")
plot(fitdist(entomo$ab.cavspe,"nbinom")) # oui
# 
fitp<-fitdist(entomo$ab.cavspe,"pois")
plot(fitp) # 
#
fitn<-fitdist(entomo$ab.cavspe,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$ab.cavspe,"lnorm")
plot(fitlnorm) # 
#
fitexp<-fitdist(entomo$ab.cavspe,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$ab.cavspe,"geom")
plot(fitgeom) # oui
#
fitbeta<-fitdist(entomo$ab.cavspe,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$ab.cavspe,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$ab.cavspe,"gamma",method="mme")
plot(fitg) # 
#
modelABCAVspe <- glmmTMB(
  ab.cavspe ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "nbinom1", 
  data = Final, 
  na.action = na.fail) ; summary(modelABCAVspe)
#
modelABCAVspe2 <- glmmTMB(
  ab.cavspe ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "nbinom1", 
  data = Final, 
  na.action = na.fail) ; summary(modelABCAVspe2)
#
AICc(modelABCAVspe) # 308.9908
AICc(modelABCAVspe2) # 316.8113
#
anova(modelABCAVspe,modelABCAVspe2) # 0.001956 **
#
sim<-simulateResiduals(modelABCAVspe)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelABCAVspe, linfct = mcp(strate = "Tukey"))
summary(RES) # 0.00165 **
```

## 5.30. sr_cavspe

```{r}
descdist(entomo$sr.cavspe,discrete=TRUE,boot=1001)
plot(fitdist(entomo$sr.cavspe,"norm")) #
#
fitnb<-fitdist(entomo$sr.cavspe,"nbinom")
plot(fitdist(entomo$sr.cavspe,"nbinom")) # 
# 
fitp<-fitdist(entomo$sr.cavspe,"pois")
plot(fitp) # oui
#
fitn<-fitdist(entomo$sr.cavspe,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$sr.cavspe,"lnorm")
plot(fitlnorm) # 
#
fitexp<-fitdist(entomo$sr.cavspe,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$sr.cavspe,"geom")
plot(fitgeom) # 
#
fitbeta<-fitdist(entomo$sr.cavspe,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$sr.cavspe,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$sr.cavspe,"gamma",method="mme")
plot(fitg) # 
#
modelSRCAVspe <- glmmTMB(
  sr.cavspe ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "genpois", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRCAVspe)
#
modelSRCAVspe2 <- glmmTMB(
  sr.cavspe ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "genpois", 
  data = Final, 
  na.action = na.fail) ; summary(modelSRCAVspe2)
#
AICc(modelSRCAVspe) # 245.6462
AICc(modelSRCAVspe2) # 249.4546
#
anova(modelSRCAVspe,modelSRCAVspe2) # 0.01268 *
#
sim<-simulateResiduals(modelSRCAVspe)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelSRCAVspe, linfct = mcp(strate = "Tukey"))
summary(RES) # 0.00184 **
```

## 5.31. CWM_diam

```{r}
descdist(entomo$CWM_diam,discrete=TRUE,boot=1001)
plot(fitdist(entomo$CWM_diam,"norm")) #
#
fitnb<-fitdist(entomo$CWM_diam,"nbinom")
plot(fitdist(entomo$CWM_diam,"nbinom")) # 
# 
fitp<-fitdist(entomo$CWM_diam,"pois")
plot(fitp) # 
#
fitn<-fitdist(entomo$CWM_diam,"norm")
plot(fitn) # oui
#
fitlnorm<-fitdist(entomo$CWM_diam,"lnorm")
plot(fitlnorm) # oui
#
fitexp<-fitdist(entomo$CWM_diam,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$CWM_diam,"geom")
plot(fitgeom) # 
#
fitbeta<-fitdist(entomo$CWM_diam,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$CWM_diam,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$CWM_diam,"gamma",method="mme")
plot(fitg) # oui
#
modelCWM_diam <- glmmTMB(
  CWM_diam ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "lognormal",
  data = Final, 
  na.action = na.fail) ; summary(modelCWM_diam)
#
modelCWM_diam2 <- glmmTMB(
  CWM_diam ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "lognormal",
  data = Final, 
  na.action = na.fail) ; summary(modelCWM_diam2)
#
AICc(modelCWM_diam) # 54.16182
AICc(modelCWM_diam2) # 52.11842
#
anova(modelCWM_diam, modelCWM_diam2) # 0.1734
#
sim<-simulateResiduals(modelCWM_diam)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelCWM_diam, linfct = mcp(strate = "Tukey"))
summary(RES) # 0.00184 **
```

## 5.32. CWM_canop

```{r}
descdist(entomo$CWM_canop,discrete=TRUE,boot=1001)
plot(fitdist(entomo$CWM_canop,"norm")) #
#
fitnb<-fitdist(entomo$CWM_canop,"nbinom")
plot(fitdist(entomo$CWM_canop,"nbinom")) # 
# 
fitp<-fitdist(entomo$CWM_canop,"pois")
plot(fitp) # 
#
fitn<-fitdist(entomo$CWM_canop,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$CWM_canop,"lnorm")
plot(fitlnorm) # 
#
fitexp<-fitdist(entomo$CWM_canop,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$CWM_canop,"geom")
plot(fitgeom) # 
#
fitbeta<-fitdist(entomo$CWM_canop,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$CWM_canop,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$CWM_canop,"gamma",method="mme")
plot(fitg) # 
#
?glmmTMB
modelCWM_canop <- glmmTMB(
  CWM_canop ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = gaussian(), 
  data = Final, 
  na.action = na.fail) ; summary(modelCWM_canop)

library(lme4)
modelCWM_canop <- glmer(CWM_canop ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)),
  data = Final,
  family = Gamma(),
  na.action = na.fail)
?lme4
#
modelCWM_canop2 <- glmmTMB(
  CWM_canop ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = gaussian(link = "log"), 
  data = Final, 
  na.action = na.fail) ; summary(modelCWM_canop2)
#
AICc(modelCWM_canop) # 54.16182
AICc(modelCWM_canop2) # 52.11842
#
anova(modelCWM_canop, modelCWM_canop2) # 0.1734
#
sim<-simulateResiduals(modelCWM_canop)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelCWM_canop2, linfct = mcp(strate = "Tukey"))
summary(RES) # 0.00184 **
```

## 5.33. CWM_decay

```{r}
descdist(entomo$CWM_decay,discrete=TRUE,boot=1001)
plot(fitdist(entomo$CWM_decay,"norm")) #
#
fitnb<-fitdist(entomo$CWM_decay,"nbinom")
plot(fitdist(entomo$CWM_decay,"nbinom")) # 
# 
fitp<-fitdist(entomo$CWM_decay,"pois")
plot(fitp) # 
#
fitn<-fitdist(entomo$CWM_decay,"norm")
plot(fitn) # oui
#
fitlnorm<-fitdist(entomo$CWM_decay,"lnorm")
plot(fitlnorm) # oui
#
fitexp<-fitdist(entomo$CWM_decay,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$CWM_decay,"geom")
plot(fitgeom) # 
#
fitbeta<-fitdist(entomo$CWM_decay,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$CWM_decay,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$CWM_decay,"gamma",method="mme")
plot(fitg) # oui
#
modelCWM_decay <- glmmTMB(
  CWM_decay ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "gaussian",
  data = Final, 
  na.action = na.fail) ; summary(modelCWM_decay)
#
modelCWM_decay2 <- glmmTMB(
  CWM_decay ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "gaussian",
  data = Final, 
  na.action = na.fail) ; summary(modelCWM_decay2)
#
AICc(modelCWM_decay) # 13.54536
AICc(modelCWM_decay2) # 17.59597
#
anova(modelCWM_decay, modelCWM_decay2) # 0.01134 *
#
sim<-simulateResiduals(modelCWM_decay)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelCWM_decay, linfct = mcp(strate = "Tukey"))
summary(RES) # 0.00184 **
```

## 5.34. CWM_size

```{r}
descdist(entomo$CWM_size,discrete=TRUE,boot=1001)
plot(fitdist(entomo$CWM_size,"norm")) #
#
fitnb<-fitdist(entomo$CWM_size,"nbinom")
plot(fitdist(entomo$CWM_size,"nbinom")) # 
# 
fitp<-fitdist(entomo$CWM_size,"pois")
plot(fitp) # 
#
fitn<-fitdist(entomo$CWM_size,"norm")
plot(fitn) # oui
#
fitlnorm<-fitdist(entomo$CWM_size,"lnorm")
plot(fitlnorm) # oui
#
fitexp<-fitdist(entomo$CWM_size,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$CWM_size,"geom")
plot(fitgeom) # 
#
fitbeta<-fitdist(entomo$CWM_size,"beta") # Pas possible, pas 0/1
plot(fitbeta)
#
fitunif<-fitdist(entomo$CWM_size,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$CWM_size,"gamma",method="mme")
plot(fitg) # oui
#
modelCWM_size <- glmmTMB(
  CWM_size ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "lognormal",
  data = Final, 
  na.action = na.fail) ; summary(modelCWM_size)
#
modelCWM_size2 <- glmmTMB(
  CWM_size ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = "lognormal",
  data = Final, 
  na.action = na.fail) ; summary(modelCWM_size2)
#
AICc(modelCWM_size) # 252.6993
AICc(modelCWM_size2) # 284.6468
#
anova(modelCWM_size, modelCWM_size2) # 1.761e-08 ***
#
sim<-simulateResiduals(modelCWM_size)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelCWM_size, linfct = mcp(strate = "Tukey"))
summary(RES) # < 0.001 ***
```

## 5.35. FD_diam

```{r}
descdist(entomo$FD_diam,discrete=TRUE,boot=1001)
plot(fitdist(entomo$FD_diam,"norm")) #
#
fitnb<-fitdist(entomo$FD_diam,"nbinom")
plot(fitdist(entomo$FD_diam,"nbinom")) # 
# 
fitp<-fitdist(entomo$FD_diam,"pois")
plot(fitp) # 
#
fitn<-fitdist(entomo$FD_diam,"norm")
plot(fitn) # oui
#
fitlnorm<-fitdist(entomo$FD_diam,"lnorm")
plot(fitlnorm) # 
#
fitexp<-fitdist(entomo$FD_diam,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$FD_diam,"geom")
plot(fitgeom) # 
#
fitbeta<-fitdist(entomo$FD_diam,"beta") # oui
plot(fitbeta)
#
fitunif<-fitdist(entomo$FD_diam,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$FD_diam,"gamma",method="mme")
plot(fitg) # 
#
modelFD_diam <- glmmTMB(
  FD_diam ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = gaussian(),
  data = Final, 
  na.action = na.fail) ; summary(modelFD_diam)
#
modelFD_diam2 <- glmmTMB(
  FD_diam ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = gaussian(),
  data = Final, 
  na.action = na.fail) ; summary(modelFD_diam2)
#
AICc(modelFD_diam) # -30.42083
AICc(modelFD_diam2) # -25.66816
#
anova(modelFD_diam, modelFD_diam2) # 0.008195 **
#
sim<-simulateResiduals(modelFD_diam)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelFD_diam, linfct = mcp(strate = "Tukey"))
summary(RES) # < 0.001 ***
```

## 5.36. FD_canop

```{r}
descdist(entomo$FD_canop,discrete=TRUE,boot=1001)
plot(fitdist(entomo$FD_canop,"norm")) #
#
fitnb<-fitdist(entomo$FD_canop,"nbinom")
plot(fitdist(entomo$FD_canop,"nbinom")) # 
# 
fitp<-fitdist(entomo$FD_canop,"pois")
plot(fitp) # 
#
fitn<-fitdist(entomo$FD_canop,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$FD_canop,"lnorm")
plot(fitlnorm) # 
#
fitexp<-fitdist(entomo$FD_canop,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$FD_canop,"geom")
plot(fitgeom) # 
#
fitbeta<-fitdist(entomo$FD_canop,"beta") # 
plot(fitbeta)
#
fitunif<-fitdist(entomo$FD_canop,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$FD_canop,"gamma",method="mme")
plot(fitg) # 
#
modelFD_canop <- glmmTMB(
  FD_canop ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = beta_family(),
  data = Final, 
  na.action = na.fail) ; summary(modelFD_canop)
#
modelFD_canop2 <- glmmTMB(
  FD_canop ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = beta_family(),
  data = Final, 
  na.action = na.fail) ; summary(modelFD_canop2)
#
AICc(modelFD_canop) # -22.90872
AICc(modelFD_canop2) # -18.33089
#
anova(modelFD_canop, modelFD_canop2) # 0.008195 **
#
sim<-simulateResiduals(modelFD_canop)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelFD_canop, linfct = mcp(strate = "Tukey"))
summary(RES) # < 0.001 ***
```

## 5.37. FD_decay

```{r}
descdist(entomo$FD_decay,discrete=TRUE,boot=1001)
plot(fitdist(entomo$FD_decay,"norm")) #
#
fitnb<-fitdist(entomo$FD_decay,"nbinom")
plot(fitdist(entomo$FD_decay,"nbinom")) # 
# 
fitp<-fitdist(entomo$FD_decay,"pois")
plot(fitp) # 
#
fitn<-fitdist(entomo$FD_decay,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$FD_decay,"lnorm")
plot(fitlnorm) # 
#
fitexp<-fitdist(entomo$FD_decay,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$FD_decay,"geom")
plot(fitgeom) # 
#
fitbeta<-fitdist(entomo$FD_decay,"beta") # 
plot(fitbeta)
#
fitunif<-fitdist(entomo$FD_decay,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$FD_decay,"gamma",method="mme")
plot(fitg) # 
#
modelFD_decay <- glmmTMB(
  FD_decay ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = beta_family(),
  data = Final, 
  na.action = na.fail) ; summary(modelFD_decay)
#
modelFD_decay2 <- glmmTMB(
  FD_decay ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = beta_family(),
  data = Final, 
  na.action = na.fail) ; summary(modelFD_decay2)
#
AICc(modelFD_decay) # -142.3195
AICc(modelFD_decay2) # -132.0498
#
anova(modelFD_decay, modelFD_decay2) # 0.0006155 ***
#
sim<-simulateResiduals(modelFD_decay)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelFD_decay, linfct = mcp(strate = "Tukey"))
summary(RES) # < 0.001 ***
```

## 5.38. FD_size

```{r}
descdist(entomo$FD_size,discrete=TRUE,boot=1001)
plot(fitdist(entomo$FD_size,"norm")) #
#
fitnb<-fitdist(entomo$FD_size,"nbinom")
plot(fitdist(entomo$FD_size,"nbinom")) # 
# 
fitp<-fitdist(entomo$FD_size,"pois")
plot(fitp) # 
#
fitn<-fitdist(entomo$FD_size,"norm")
plot(fitn) # 
#
fitlnorm<-fitdist(entomo$FD_size,"lnorm")
plot(fitlnorm) # 
#
fitexp<-fitdist(entomo$FD_size,"exp")
plot(fitexp)
#
fitgeom<-fitdist(entomo$FD_size,"geom")
plot(fitgeom) # 
#
fitbeta<-fitdist(entomo$FD_size,"beta") # 
plot(fitbeta)
#
fitunif<-fitdist(entomo$FD_size,"unif")
plot(fitunif)
#
fitg<-fitdist(entomo$FD_size,"gamma",method="mme")
plot(fitg) # 
#
modelFD_size <- glmmTMB(
  FD_size ~ strate + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = lognormal(),
  data = Final, 
  na.action = na.fail) ; summary(modelFD_size)
#
modelFD_size2 <- glmmTMB(
  FD_size ~ 1 + (1 | LOCA / placette) + offset(log(prop.trap.season)), 
  family = lognormal(),
  data = Final, 
  na.action = na.fail) ; summary(modelFD_size2)
#
AICc(modelFD_size) # -74.84554
AICc(modelFD_size2) # -41.68336
#
anova(modelFD_size, modelFD_size2) # 9.738e-09 ***
#
sim<-simulateResiduals(modelFD_size)
testUniformity(sim)
testOutliers(sim)
#
RES<-glht(modelFD_size, linfct = mcp(strate = "Tukey"))
summary(RES) # < 0.001 ***
```

